package NOV_01.constructor_overloading;

public class Calculate {
	public Calculate(int x, int y ) 
	{
		System.out.println("Sum of "+x+" and "+y+" is: " + (x+y));
	}
	public Calculate(int x) {
		//this(100, 200);
		System.out.println("Square of "+x+" is: " +x*x);

	}
	
}
